#! /usr/bin/env python

import unittest

import app
"""
from app import welcome, calculate_delivery_cost, construct_response_body


class Testing(unittest.TestCase):
    
    def test_welcome(self):
        re_msg = welcome()
        ex_msg = 'Welcome. Please append "/quotes" to the URL to get quotes.'
        self.assertEqual(re_msg, ex_msg)


    def test_calculate_delivery_cost(self):
        returned = calculate_delivery_cost('SW1A1AA', 'EC2A3LT', 1.1)
        expected = 349
        self.assertEqual(returned, expected)


    def test_construct_response_body(self):
        returned = construct_response_body('a', 'b', 'c', 'd')
        expected = {'pickup_postcode': 'a',
                    'delivery_postcode': 'b',
                    'price': 'c',
                    'vehicle': 'd'}
        self.assertEqual(returned, expected)


if __name__ == '__main__':
    unittest.main()
"""
